model RoadMap {
  id          Int       @id @default(autoincrement())
  title       String
  description String
  date        DateTime?
}

model Banner {
  id    Int     @id @default(autoincrement())
  title String
  show  Boolean
}
